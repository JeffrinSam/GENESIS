name: genesis-generation
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - ffmpeg
  - pip:
      - anthropic>=0.21.0
      - flask>=2.3.0
      - werkzeug>=2.3.0
      - requests>=2.28.0
      - numpy>=1.24.0
      - torch>=2.0.0
      - torchvision>=0.15.0
      - pillow>=9.0.0
      - opencv-python>=4.7.0
      - tqdm>=4.65.0
      - pyyaml>=6.0
      - transformers>=4.30.0
      - accelerate>=0.20.0
      - diffusers>=0.21.0
      - einops>=0.6.0
      - python-dotenv>=1.0.0

# Usage:
#   conda env create -f environments/environment-generation.yml
#   conda activate genesis-generation
# Required for: Part 1 — ClaudeOpusBrain, AgentLLM
